import { BrowserRouter } from 'react-router'
import RoutesConfig from './routes'

const App = () => {
    return (
        <BrowserRouter>
            <RoutesConfig />
        </BrowserRouter>
    )
}

export default App
